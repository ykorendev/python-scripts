

# Text Analyzer Script

## Descrition 
Reads a text file, counts the number of lines and words,
and writes the results to a report file.

## Usage 
```bash
python3 analyze_text.py <input_file>
```
**Output:**

Creates a report.txt file containing the analysis results.



## Notes
if the input file does not exits, the script displays error message.
